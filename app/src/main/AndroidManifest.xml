<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joonhuiwong.mylibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLibrary">
        <activity android:name=".WebsiteActivity" />
        <activity
            android:name=".FavoriteBookActivity"
            android:label="@string/favorite_books_label" />
        <activity
            android:name=".CurrentlyReadingBookActivity"
            android:label="@string/currently_reading_books_label" />
        <activity
            android:name=".WantToReadBookActivity"
            android:label="@string/want_to_read_books_label" />
        <activity
            android:name=".AlreadyReadBookActivity"
            android:label="@string/already_read_books_label" />
        <activity android:name=".BookActivity" />
        <activity
            android:name=".AllBooksActivity"
            android:label="@string/all_books_label" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>